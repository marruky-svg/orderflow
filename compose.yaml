services:
  postgres:
    image: 'postgres:16'
    environment:
      - POSTGRES_DB=orderflow_db
      - POSTGRES_PASSWORD=orderflow_pass
      - POSTGRES_USER=orderflow_user
    ports:
      - '5432:5432'